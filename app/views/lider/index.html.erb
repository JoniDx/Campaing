<%= nav_dashboard current_user.rol %>
<p id="notice" class=""><%= notice %></p>


<div class="container">
<%= link_to 'Agregar lider', lider_create_path, class:"btn btn-block btn-outline-primary mb-5" %>

  <div class="card">
    <div class="card-body">
      <table class="table">
        <thead>
          <tr>
              <th scope="col">Correo</th>
              <th scope="col">Campaña</th>
              <th scope="col">Editar</th>
          </tr>
        </thead>
        <tbody>
          <% @lideres.each do |lider| %>

          <tr>
              <th scope="row"><%= lider.email %></th>
              <th scope="row"><%=  lider.campaign[0].name %></th>
              <th scope="row">
                <%= link_to "Editar", "/lider/edit/#{lider.id}" %>
              </th>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>          
  </div>          
</div>          